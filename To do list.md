# To Do List

记录想做的事情，完成后勾选。每件事是一个编号项（#1、#2、#3……），下面的 #.1、#.2 是这件事里具体要做的子任务。

## 目录

- [1. 股票下单本地时间校准实验](#1-股票下单本地时间校准实验)
  - [1.1 GPS 接收模块授时校准](#11-gps-接收模块授时校准)
  - [1.2 调试 FPGA 网卡硬件时间戳](#12-调试-fpga-网卡硬件时间戳)
- [2. Dell OptiPlex 7040 MT 内存升级备忘](#2-dell-optiplex-7040-mt-内存升级备忘)
  - [2.1 购买内存条](#21-购买内存条)
  - [2.2 安装内存条](#22-安装内存条)

# 1. 股票下单本地时间校准实验

- [ ] 状态：待办

### 背景与动机

起因是在[集思录论坛的一个讨论帖](https://www.jisilu.cn/question/524979?show_all_answer-TRUE__item_id-5534295__answer_id-5534295__single-TRUE#!answer_5534295)看到关于"下单时间校准"的争论：

- **观点一**（豫章秋水）：NTP 校时能到 ±10ms，已经远超个人交易需求；真正的瓶颈在于链路后端的网络抖动（±50ms 量级），基于网络的授时如果要更靠谱应该用 PTP（IEEE 1588v2），但对个人来说不如直接外挂一个 GPS。
- **观点二**（海55924）：可以用一个便宜的 USB GPS 模块（VK-162，约20元，拼多多有售）接收 GPS 时间码，配合脚本在开盘前校准本地时钟，精度能到毫秒级，远高于公网 NTP。他同时指出，这只能解决本地时钟与真实时间的同步问题，对本地到交易所的网络延迟、以及行情 tick 的接收延迟没有帮助。

手头正好有一块已经跑通 PTP 硬件时间戳的 FPGA 网卡（AS02MC04 / XCKU3P，基于 taxi/Corundum 框架），这让这个原本只是"个人校时"的小需求，变成一个可以顺手验证、并延伸出更有意思的硬件时间戳实验的机会：既能验证 GPS 授时方案的实际精度，也能利用现有的网卡硬件时间戳能力，进一步量化真实的网络处理延迟——这比单纯"对齐时钟"更有实际价值。

### 目标

分两阶段推进，先低成本验证需求，再决定是否投入硬件方案：

1. **验证**：本地时钟通过 GPS 授时能达到的实际精度是多少
2. **延伸**：能否用已有的 FPGA 网卡硬件时间戳能力，量化本机网络处理延迟

## 1.1 GPS 接收模块授时校准

- [ ] 购买并配置 GPS 模块，验证本地时钟校准精度

**硬件**
- GPS 接收模块（VK-162 或同类 u-blox NEO-6/7 芯片模块，约 20 元）
- 注意确认模块是否带独立 PPS 输出——只有 NMEA 串口 vs 带 PPS，直接决定后续能否做微秒级对齐

**软件**
- `gpsd`：接收和解析 GPS 数据，不自己写解析逻辑
- `chrony`（而非 ntpd）：将 gpsd 配置为本地时钟参考源，实际执行校时

**验收标准**
- `chronyc tracking` 输出的本地时钟相对 GPS 参考的实际稳定精度（offset / 抖动统计）

## 1.2 调试 FPGA 网卡硬件时间戳

- [ ] 在 1.1 数据基础上决定是否投入，量化本机网络处理延迟

- 若 GPS 模块带 PPS 输出，接入 FPGA 卡空闲 GPIO，写一个边沿计数模块，用 FPGA 内部时钟标定网卡 PTP 时钟域相对 GPS（真实 UTC）的绝对偏差
- 复用已跑通的 PTP 硬件时间戳基础设施，对实际收发报文打点：网口硬件时间戳 vs 应用层软件时间戳，两者之差即为本机处理延迟的真实数据

### 执行顺序

1.1（纯软件，成本低，一两天出结果）→ 得到具体精度数字后再决定是否投入 1.2 的硬件实验。

### 参考资料

- [集思录讨论帖](https://www.jisilu.cn/question/524979?show_all_answer-TRUE__item_id-5534295__answer_id-5534295__single-TRUE#!answer_5534295)
- FPGA NIC 相关基础设施：AS02MC04 (XCKU3P)，taxi/Corundum 框架，cndm 驱动

# 2. Dell OptiPlex 7040 MT 内存升级备忘

- [ ] 状态：待办

### 硬件配置

* CPU：Intel Core i7-6700（Skylake-S）
* 主板：Dell 0Y7WYT，Q170芯片组
* 平台内存上限：DDR4-2133（更高频条子会被降频运行）
* 内存插槽：共4条（DIMM1-4），当前占用2条（DIMM3、DIMM4），空闲2条

### 当前已装内存

1. Micron MTA8ATF51264AZ-2G1B1，4GB，DDR4-2666（PC4-21333），1Rx8，非ECC UDIMM
2. TeamGroup TED4G4G2133C15BK，4GB，DDR4-2133，CL15-15-15-36，1.2V，非ECC UDIMM

两条容量相同、品牌不同，混插后系统识别为 8GB 双通道，实际运行在1067MHz(DDR4-2133)，工作正常（此前一度显示单通道/4GB是插座接触不良所致，重新插紧后恢复正常）。

## 2.1 购买内存条

- [ ] 按升级方案购买新内存条

* 加装：DDR4 UDIMM，非ECC，频率2133/2400/2666均可（会降频到2133）
* 插入剩余2个空插槽（DIMM1、DIMM2）
* 建议买2条同型号同容量新条子，避免继续混插
   * 轻度需求：2×4GB → 总容量16GB
   * 中高需求：2×8GB → 总容量24GB
* 购买时认准：DDR4、UDIMM（不是笔记本用的SODIMM）、非ECC、频率≥2133

## 2.2 安装内存条

- [ ] 拆机、插内存条并验证识别正常

* 机箱侧面硬盘/光驱架需要先拉开"PULL"卡扣再旋转打开，才能看到内存插槽
* 插内存条时要垂直插到底，听到两侧卡扣"咔"一声扣紧，否则可能出现识别不到或容量异常的情况
* 加装后开机若提示"The amount of system memory has changed"，是正常提示，按F1继续启动即可
